<h1>Interests#index</h1>
<p>Find me in app/views/interests/index.html.erb</p>


<% @learn_interests.each do |interest| %>
  <%= "#{interest.user.name} wants to learn #{interest.cuisine.name}"%>
<% end %>

<% @teach_interests.each do |interest| %>
  <%= "#{interest.user.name} wants to teach #{interest.cuisine.name}"%>
<% end %>
